문제 : 
 template class 는 std::iterator 로 저장불가 
-> 저장할 다른 방법이 필요 

지금 필요한 정보 
entity에 어떤 component가 붙어있는지 알아야 함 그리고 각각의 component들을 각각의 type으로 가져와야함 

-> 
이 entity가 어떤 component를 가지는지 family로만 가지고 있도록 하고 famliy 배열? 
map<entity, list<int64_t>> 이렇게 가짐 -> 이걸로 이녀석이 진짜 이 component를 가지고 있는지 검사 

component manager에서 component들과 
어떤 entity가 어떤 component와 대응되는지 
array<component >
+
map<entity, componentinstance[int64_t 그냥 index]> 이렇게 가지도록 함 오호우으 ㅣ웨에ㅣ 

=> 이렇게 되면 component 찾는과정은 
1. entity에게 질의 이 component 있냐
2. 있으면 world_에게 질의해서  해당 component manager에게 component 가져옴  


=> 문제 
component handle을 .. 어떻게 변수로 가져오지 이건 일단 복사하자 >< 